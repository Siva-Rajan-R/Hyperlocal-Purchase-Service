services:
  purchase-service:
    build: 
      context: .
      dockerfile: .
    
    env_file:
      - .env
    
    networks:
      - backend-networks
    
    ports:
      - "8003:8000"
    
    restart: unless-stopped